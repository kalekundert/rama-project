# Example function for adding vectors
def add(v1, v2):
    # v1 and v2 are both 3d vectors represented as tuples of floats
    # e.g. v1 = (1.0, 2.4, -3.9)
    return [v1[0] + v2[0], v1[1] + v2[1], v1[2] + v2[2]]

# Write more functions to do vector math! (dot product, etc.)
